# RETIRED: Sentinel now has one deployable runtime image: Dockerfile.sentinel.
# Broker-capable code and the execution-capability marker are built into that
# single immutable runtime. This tombstone intentionally has no FROM instruction,
# so any stale build path that still attempts to create a second authorized
# runtime fails closed at Dockerfile parsing/build time.
