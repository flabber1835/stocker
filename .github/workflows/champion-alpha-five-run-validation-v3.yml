name: Champion corrected alpha second five-run validation
run-name: Champion corrected alpha second five-run validation guard - ${{ github.sha }}
on:
  push:
    branches: [research/champion-alpha-five-run-validation-v3]
    paths:
      - '.github/workflows/champion-alpha-five-run-validation-v3.yml'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: champion-alpha-five-run-v3-${{ github.ref }}
  cancel-in-progress: false
jobs:
  experiment:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Duplicate launch suppressed; authoritative five-arm run is 34076988256.'
