name: PIT Russell archive experiment

on:
  push:
    branches:
      - research/pit-russell-archive-reconstruction
    paths:
      - 'research/pit_russell_archive/**'
      - 'tools/pit_russell_archive_probe.py'
      - 'tests/scripts/test_pit_russell_archive_probe.py'
      - '.github/workflows/pit-russell-archive-experiment.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deterministic-tests:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Check out exact experiment revision
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 1
      - name: Set up Python 3.12
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
          check-latest: false
      - name: Verify isolation and deterministic probe tests
        run: |
          set -euo pipefail
          test "${GITHUB_REF_NAME}" = "research/pit-russell-archive-reconstruction"
          python -m unittest tests.scripts.test_pit_russell_archive_probe -v

  component-probe:
    needs: deterministic-tests
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: r1000-exact
            index: '1000'
            query: 'https://www.russell.com/indexes/documents/Membership/Russell1000_Membership_List.pdf'
          - label: r1000-wildcard
            index: '1000'
            query: 'http://www.russell.com/indexes/*Russell1000*Membership*'
          - label: r2000-exact
            index: '2000'
            query: 'https://www.russell.com/indexes/documents/Membership/Russell2000_Membership_List.pdf'
          - label: r2000-wildcard
            index: '2000'
            query: 'http://www.russell.com/indexes/*Russell2000*Membership*'
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - name: Check out exact experiment revision
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 1
      - name: Set up Python 3.12
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
          check-latest: false
      - name: Probe archived Russell component list
        run: |
          set -euo pipefail
          out="/tmp/${{ matrix.label }}"
          rm -rf "$out"
          python tools/pit_russell_archive_probe.py \
            --from-year 2005 \
            --to-year 2010 \
            --max-downloads-per-year 2 \
            --timeout 20 \
            --attempts 3 \
            --delay 0.4 \
            --url '${{ matrix.query }}' \
            --output-dir "$out"
      - name: Publish evidence summary
        if: always()
        run: |
          report="/tmp/${{ matrix.label }}/report.md"
          if [ -f "$report" ]; then
            cat "$report" >> "$GITHUB_STEP_SUMMARY"
          fi
      - name: Upload provenance evidence only
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: pit-russell-${{ matrix.label }}-${{ github.sha }}
          path: |
            /tmp/${{ matrix.label }}/manifest.json
            /tmp/${{ matrix.label }}/summary.json
            /tmp/${{ matrix.label }}/captures.csv
            /tmp/${{ matrix.label }}/report.md
          if-no-files-found: warn
          retention-days: 30
