# Held/pending factual review — common confirmations batch 01

Date: 2026-09-06

Status: **8 factual common-equity confirmations recorded; queue application pending.**

Confirmed as common under the existing Champion semantics: `VAL1`, `EV1`, `UST1`, `NVO`, `ATI`, `RACE`, `KNX1`, `ESLT`.

These decisions use security class, CUSIP/identity and issuer/SEC filing evidence only. No returns, ranks, realized portfolio outcomes, survival or later index membership are used.

`FMX` remains open because its ADS represents BD Units composed of several Mexican share series. That structure needs an explicit policy confirmation before it is closed as common; the review will not silently broaden the existing common-equity definition.

Machine-readable evidence: `held-pending-common-batch-01.json`.
