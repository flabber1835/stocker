# Continuation checkpoint

Recorded 2026-09-06. Branch: `research/champion-certification-economic-integrity`.

## Established verdict and published work

**Outcome E / certification HOLD.** The completed semantic audit and 147-session controlled evidence are indexed in [README.md](README.md). The three required narrative reports, 60-rule specification inventory, machine-readable attribution register, raw observations and reproduction instructions are published.

Complete evidence commit: `ede62f51d2047394b8b1fa089a6cb848beffa2c7` (142 hash-listed files). Additional core evidence commit: `14ba19b7ec26201d65de271a7b35556b2c6bb95b` (89 common files with identical payload hashes). Both original measured prefixes and all ten original metric-report rows have been reproduced by the offline validators.

Frozen profile, starting shadow capital, canonical corpus and production/main remain unchanged by this investigation. No final replacement certificate has been issued.

## Existing full-horizon capacity-only diagnostic

Run: [34044640055](https://github.com/flabber1835/stocker/actions/runs/34044640055), attempt 1.

Source: `9924bfa5b36322a5bf78296f42544eaa175299d8`.

Workflow: `.github/workflows/champion-economic-full-capacity-control.yml`.

Runner: `backtester/champion_economic_full_capacity_control.py`.

Last inspected state: **in progress**, executing the full-horizon one-factor replay after exact source/runtime/package verification. Treat this as a dated checkpoint and re-read the run before stating current status.

The runner builds the exact certificate program at `27bb992087182c42c3c051e62bf837895f5d2ab7` and removes exactly the two whole-order participation guards. The original $100M, profile, dates, corpus, classifier, terminal policy, costs and 15-session dividend lag stay fixed. This extends the already identified capacity mechanism to the full horizon. It is conditional attribution and cannot approve the remaining inherited economics.

## Offline reporting and persistence

Run: [34045461973](https://github.com/flabber1835/stocker/actions/runs/34045461973).

Source: `35e5d6c060e7862551621e5d5e5e59da03276ef9`.

Workflow: `.github/workflows/champion-full-capacity-offline-report.yml`.

Reporter: `backtester/champion_full_capacity_report.py`, introduced at `fcdcd2858784d8e843de55bd4f1fa7a5cea46731`.

This workflow observes the existing controlled run. It does not start a historical replay. It first verifies the 89-file immutable baseline manifest and reproduces all ten original metric rows using the exact historical `metric_block` function, SHA256 `570b44333db93b3ec6a6b1cfbc857cbbf6337a9448ee5507323933c028ecaff5`.

A full successful raw replay must then pass exact identity/hash checks, normalized source-AST equality to the two validated prefix cases, reproduction of all 30 controlled prefix columns, a 5,032-session calendar match, identical SPY path, and identical eligible counts and durable-ranking hashes. The reporter computes the same fixed 5/10/15/20-year and actual-elapsed max-window metrics and labels their differences conditional and uncertified.

Expected persistence location: `research/champion-economic-integrity/full-capacity-control/`. That location is created by the reporting workflow after the existing run completes or a preserved failure is recorded. Inspect its `STATUS.md` and manifest when available; verify the workflow conclusion as well.

## Reporting defects already identified

The full raw runner emits `engine/daily.csv` and `engine/summary.json`. Its original workflow comparator expects a `metrics.csv` that the raw runner does not create. The comparator also seeks lowercase `research`, while the original report's role is uppercase `RESEARCH`. The separate offline reporter resolves these reporting seams by consuming retained daily bytes and the exact original metric function.

A failed original comparison step after a successful raw replay is not a reason to repeat the historical replay. Preserve the original workflow status and diagnose the raw replay and offline validator separately.

The full-run workflow also leaves `RESEARCH_REPLAY_MODE` unset. In the exact generated canonical program, MODE/PIT_MODE are used for logging/summary labels; canonical loaders and economic branches are independent of those labels. Consequently a raw `nonpit` summary label can coexist with the pinned canonical data program. The offline report preserves that label and separately verifies economic source and corpus identities. It does not use the label to assert PIT certification.

## Scope that remains open

The completed prefix contains no held dividend, sale or terminal event. The dividend-lag control is unexercised. The full-capacity extension supplies one conditional return effect if validated; it does not supply full-horizon independent cash/share/claim accounting, complete candidate-versus-certificate attribution, or an authoritative resolution of the economic contract.

The documented 1-versus-15 dividend settlement conflict, historical classification authority, terminal/leadership/unknown-mark conventions, C1 recovery, cash model and shadow/deployment-capital interpretation still require resolution. The shared open-dividend boundary and candidate publication-availability defect require correctness repairs. The exact generated Champion requires accounting, causality and complete-state resume proof before final certification.

## Exact continuation procedure

Read current branch head, both run states and their actual artifact metadata first. Consume already-completed artifacts and the immutable evidence manifests. Preserve concurrent branch additions. Validate a completed full-capacity artifact through the offline reporter and append a dated conditional-attribution finding. Retain outcome E/HOLD until the intended contract and correctness proof are established. No parameter tuning, capital change, production promotion or merge into main is authorized by this checkpoint.
