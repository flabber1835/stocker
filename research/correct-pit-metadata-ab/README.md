# Corrected PIT metadata A/B evidence

This directory supersedes the retracted issue #241 experiment.

Primary report: `RESULTS.md`

Machine-readable outputs:

- `metrics.csv` — standard 5/10/15/20-year metrics for current Sharadar metadata, best-effort PIT, and SPY.
- `parity.json` — control fingerprint, category/issuer causality gates, coverage, and artifact hashes.
- `fast-trigger-diff.csv` — the five sessions where the raw fast trigger differs after PIT sector replacement.

Research branch only. Nothing here changes production strategy behavior or `main`.
