# Twenty-year comparison: slow defense at 15 sessions

Owner-authorized 2026-09-22. Registered before implementation and results.
Verified main: `ee23c894c97a2c4023654ce3a56a62728f5b061e`; isolated feature branch
`codex/slow15-twenty-year`, Git/gh PR-only delivery targeting main. No self-merge.
Required deployment, architecture, execution, certification and Wealth Core
reading is unchanged from the immediately preceding investigation on this base.

## Single frozen intervention

Change only `SLOW['dur']` from 30 to 15, using the exact research profile already
registered in PR #433 at `f2a50c7b6ff4c9686f49b6eb53863e8e2b9959bb`.
Do not optimize, change another threshold, enable owned impairment or modify the
production default. Every research snapshot binds to source plus parameter map.
Never seed its memory with a current-policy checkpoint.

## Comparable economic run

Run the canonical production session kernel once, freshly from January 3, 2006
with $100,000 and the same retained broad-universe PIT archive, classification
overlay, final supplemental action evidence and SPY/bill factors. Formation
precedes the July 31, 2006 measurement close; measurement ends July 31, 2026.
This repeats the earlier formation convention and capital, not a manually seeded
portfolio. Freeze all inputs for the whole run.

The current policy remains the embedded production controller. Independently
replay both the unchanged current research adapter and the slow15 adapter from
genesis over the actual canonical Core observations and leadership witness.
Require exact current controller target, reason, native state and signal parity
at every close. Core cannot observe either simulated account or variant target.

Use the production scalar accountant independently for both accounts. The old
allocation bears overnight returns; the previous close's decision acts at the
next open. Same cost convention, bill factors and measurement close. Verify the
variant's daily accounting with independent Decimal factor arithmetic. Store the
Core observation stream so later exposure-only comparisons need not regenerate
the expensive unchanged Core path.

Unlike the earlier mixed-source continuation, the new canonical stream uses the
verified current source uniformly. Compare its control with the retained 29.24x
run, report any drift, and never repin that result or silently attribute a source
difference to slow15. The causal parameter comparison is the two new accounts
sharing precisely the same Core stream.

## Resume and validation

Keep the original source-bound production runner and its guards intact. A
research wrapper passively captures evidence and extends new checkpoints with
both research controllers, slow15 economics and statistics, immutable source/
input bindings and the last emitted session. The wrapper does not relabel a
production state or install a variant controller in production. Restore refuses
changed source, supplements or research identity and requires the same cursor.
Use separate numbered segments; preserve all preceding evidence. Stop on a
production/data refusal rather than inventing action inputs or accepting gaps.

Before the long run: targeted tests for parameter isolation, cross-identity
restore rejection, next-open accounting and resume integrity, with deliberate
falsifiers. During the run: exact baseline-controller parity, independent daily
accounting, state-bound checkpoints and reported progress. At completion: exact
session coverage, independent CAGR/multiple/drawdown, changed decisions and
per-episode deltas including adverse episodes; final restart check and retained
source/artifact hashes. No broad unrelated suite.

The run is provisional economic research. Known archive classifications, action
proxies and scalar execution assumptions persist. No NAS or real broker access,
no provider-finality claim and no production promotion or economic certification.
