docs/sentinel-handoff/00_README/FROZEN_SENTINEL_1P1_RULE.json text eol=lf
*.sh text eol=lf
# This CSV is a byte-pinned certification falsifier.  Treating it as text lets
# core.autocrlf change its SHA-256 in a Windows checkout before the runner can
# verify it against the bundle's SHA256SUMS.txt.
docs/sentinel-reference-implementation/sentinel_1p1_daily.csv -text
